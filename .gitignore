# Build artifacts
obj
bin

# Specific to Visual Studio
.vs

# Specific to VSCode 
# These files are autogenerated by the C# extension so they aren't needed. We will likely include recommended extensions, so we aren't ignoring the whole folder. 
.vscode/launch.json
.vscode/tasks.json
